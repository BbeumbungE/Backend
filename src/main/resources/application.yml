server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mokumoku?serverTimezone=Asia/Seoul
    username: root
    password: ehrtnfl1!

  jpa:
    hibernate:
      ddl-auto: update
    #    show-sql: true
    open-in-view: false

    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
  #        use_sql_comments: true
  redis:
    token:
      host: localhost
      port: 6380

    cache:
      host: localhost
      port: 6379

  rabbitmq:
    host: rebbitmq
    port: 5672
    username: hp5393
    password: ehrtnfl1!


  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 699b3618c10ee417e1295d6deb920562
            client-secret: 5YOG2MB948tsV6wKZUMjY8344xHLzSth
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            client-authentication-method: POST
            client-name: Kakao
            scope:
              - account_email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

  jwt:
    secret: beumbunge
    token:
      access-expiration-time: 43200000 # 12시간
      refresh-expiration-time: 604800000 # 7일

logging:
  level:
    org.hibernate:
      SQL: debug
      type: trace
